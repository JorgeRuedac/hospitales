@model hospital.Models.hospitales_servicios

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>hospitales_servicios</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.ID_hospitales_servicios)

        <div class="form-group">
            @Html.LabelFor(model => model.Cod_hospital, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("Cod_hospital")
                @Html.ValidationMessageFor(model => model.Cod_hospital)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Id_servicio, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("Id_servicio")
                @Html.ValidationMessageFor(model => model.Id_servicio)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.CodigoRefer, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.CodigoRefer)
                @Html.ValidationMessageFor(model => model.CodigoRefer)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
 
